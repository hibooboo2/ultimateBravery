{{ define "build" }}
<div class="build">
    {{ template "champion" .Champion }}
    {{ template "map" .Map }}
    {{ .Name }}
    </br>
    <a href="{{ .PermLink }}">PermLink</a>
    <div class="itemList bottom">
        {{ range .Items }}
            {{ template "itemInBuild" . }}
        {{ end }}
    </div>
</div>

{{ end }}
