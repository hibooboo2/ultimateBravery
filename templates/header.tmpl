{{define "header"}}
<html>
<head>
    <link rel='shortcut icon' href="/static/favicon.ico" type='image/x-icon' />
    {{ if .Dev }}
        <meta http-equiv="refresh" content="60">
        <link rel="stylesheet" type="text/css" href="/static/css/dev.css">
    {{ else }}
        {{ if .Dark }}
            <link rel="stylesheet" type="text/css" href="/static/css/dark-main.css">
        {{ else if .Light }}
            <link rel="stylesheet" type="text/css" href="/static/css/light-main.css">
        {{ else }}
            <link rel="stylesheet" type="text/css" href="/static/css/normal-main.css">
       {{ end }}
    {{ end }}
    <meta name="viewport" content="width=device-width" >
    <title>{{ .Name }}</title>
    <style>
        .delayShow {
            display: none;
        }
    </style>
</head>
<body>
<header class="delayShow">
    <button onclick="location.href='/'">Home</button>
    <button onclick="location.href='/items'">Items</button>
    <button onclick="location.href='/champions'">Champions</button></a>
</header>
<div class="delayShow allcontent">
{{end}}
